# Simple Hello Python Project

This is a minimal Python project that prints `Hello` to the console.

## How to run

1. **(Optional) Create and activate a virtual environment.**
2. **Install dependencies (none required, but this is the standard command):**

```bash
pip install -r requirements.txt
```

3. **Run the script:**

```bash
python main.py
```

